#!/bin/bash

# omarchy:summary=Cache background switcher thumbnails for the current theme

theme_name=$(cat "$HOME/.config/zanken/current/theme.name" 2>/dev/null)

omarchy-menu-images \
  --cache-only \
  "$HOME/.config/zanken/current/theme/backgrounds" \
  "$HOME/.config/zanken/backgrounds/$theme_name"
