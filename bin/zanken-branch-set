#!/bin/bash

# zanken:summary=Set the branch for Zanken's git repository.
# zanken:args=<main|rc|dev>

if (($# == 0)); then
  echo "Usage: zanken-branch-set [main|rc|dev]"
  exit 1
else
  branch="$1"
fi

if [[ $branch != "main" && $branch != "rc" && $branch != "dev" ]]; then
  echo "Error: Invalid branch '$branch'. Must be one of: main, rc, dev"
  exit 1
fi

git -C $ZANKEN_PATH switch $branch
