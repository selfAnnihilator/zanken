#!/bin/bash

# zanken:summary=List available monospace fonts
# zanken:examples=zanken font list | zanken font set "CaskaydiaMono Nerd Font"

fc-list :spacing=100 -f "%{family[0]}\n" | grep -v -i -E 'emoji|signwriting|omarchy' | sort -u
