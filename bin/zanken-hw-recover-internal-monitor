#!/bin/bash

# zanken:summary=Clear the internal-monitor-disable toggle if no external display is connected.

TOGGLE="$HOME/.local/state/zanken/toggles/hypr/internal-monitor-disable.lua"

if [[ -f $TOGGLE ]] && ! zanken-hw-external-monitors; then
  rm -f "$TOGGLE"
fi
