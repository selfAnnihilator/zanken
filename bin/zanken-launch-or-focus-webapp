#!/bin/bash

# zanken:summary=Launch or focus on a given web app identified by the window-pattern.
# zanken:args=<window-pattern> <url-and-flags...>

if (($# == 0)); then
  echo "Usage: zanken-launch-or-focus-webapp [window-pattern] [url-and-flags...]"
  exit 1
fi

WINDOW_PATTERN="$1"
shift
LAUNCH_COMMAND="zanken-launch-webapp $@"

exec zanken-launch-or-focus "$WINDOW_PATTERN" "$LAUNCH_COMMAND"
