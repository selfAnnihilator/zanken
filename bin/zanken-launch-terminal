#!/bin/bash

# zanken:summary=Launch a terminal in the active terminal's current directory
# zanken:args=[command...]

exec setsid uwsm-app -- xdg-terminal-exec --dir="$(zanken-cmd-terminal-cwd)" "$@"
