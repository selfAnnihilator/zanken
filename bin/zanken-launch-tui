#!/bin/bash

# zanken:summary=Launch a TUI command in the default terminal with Omarchy styling
# zanken:args=<command> [args...]

exec setsid uwsm-app -- xdg-terminal-exec --app-id=org.zanken.$(basename $1) -e "$1" "${@:2}"
