#!/bin/bash

# zanken:summary=Update packages using the current pacman configuration
# zanken:requires-sudo=true

# Allow user customization before the upgrade runs.
zanken-hook pre-refresh-pacman

sudo pacman -Syu --noconfirm
