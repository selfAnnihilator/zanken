#!/bin/bash

# zanken:summary=Overwrite the user configs for swayosd (controls on-screen feedback for changing volume/songs etc) with the Zanken defaults and restart the service.

zanken-refresh-config swayosd/config.toml
zanken-refresh-config swayosd/style.css
zanken-restart-swayosd
