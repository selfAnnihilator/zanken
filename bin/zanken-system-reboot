#!/bin/bash

# zanken:summary=Reboot after closing application windows
# zanken:examples=omarchy reboot | omarchy system reboot
# zanken:aliases=zanken reboot

zanken-state clear re*-required

# Schedule the reboot to happen after closing windows (detached from terminal)
nohup bash -c "sleep 2 && systemctl reboot --no-wall" >/dev/null 2>&1 &

# Now close all windows
zanken-niri-window-close-all
sleep 1 # Allow apps like Chrome to shutdown correctly
