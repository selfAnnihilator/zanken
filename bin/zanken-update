#!/bin/bash
# zanken:summary=Update the installed Zanken desktop release
# zanken:group=update
# zanken:args=[--fetch]

set -euo pipefail

COMMAND_DIR=$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)
exec "$COMMAND_DIR/zanken-release" update "$@"
