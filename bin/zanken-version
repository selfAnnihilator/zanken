#!/bin/bash

# zanken:summary=Print the installed release version or unmanaged source version

set -euo pipefail

COMMAND_DIR=$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)
exec "$COMMAND_DIR/zanken-release" version --plain "$@"
