#!/bin/bash

# zanken:summary=Print the source Git branch, independently of installed release mode

set -euo pipefail

COMMAND_DIR=$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)
"$COMMAND_DIR/zanken-release" status | jq -r '.branch // "detached-or-unavailable"'
