#!/bin/bash

# zanken:summary=Print the active Zanken git release channel

branch=$(git -C "$ZANKEN_PATH" branch --show-current 2>/dev/null || true)

case "$branch" in
main) echo "stable" ;;
rc) echo "rc" ;;
dev) echo "dev" ;;
*) echo "unknown" ;;
esac
